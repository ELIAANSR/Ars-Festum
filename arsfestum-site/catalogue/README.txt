Place the compressed catalogue PDF in this folder as:

    ars-festum-catalogue.pdf

The website links to /catalogue/ars-festum-catalogue.pdf

Keep it around 40 MB or less. Do NOT commit the original ~400 MB export.
If the file grows, host it on Vercel Blob or an R2/S3 bucket and update the
link in index.html instead of committing it here.
